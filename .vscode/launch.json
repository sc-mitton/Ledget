{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Oathkeeper Tests",
            "type": "go",
            "request": "launch",
            "mode": "exec",
            "cwd": "${workspaceFolder}/oathkeeper",
            "program": "${workspaceFolder}/oathkeeper/bootstrap",
            "args": [],
            "env": {
                "_LAMBDA_SERVER_PORT": "8080",
                "ENVIRONMENT": "test",
            }
        }
    ],
    "compounds": [
        {
            "name": "build and debug",
            "configurations": [
                "Oathkeeper Tests"
            ],
            "preLaunchTask": "oathkeeper-tests-setup",
            "postDebugTask": "oathkeeper-tests-cleanup"
        }
    ]
}
