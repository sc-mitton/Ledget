import { StyleSheet } from "react-native";

export default StyleSheet.create({
  splitButtons: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    gap: 10,
  },
  splitButton: {
    flex: 1,
  }
});
