import { StyleSheet } from "react-native";

export default StyleSheet.create({
  splitButtons: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: 10,
    marginTop: -8,
    marginBottom: -28
  },
  splitButton: {
    flex: 1,
  },
  header: {
    gap: 8,
    marginTop: 8,
  }
});
