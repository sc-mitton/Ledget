import { StyleSheet } from "react-native";

export default StyleSheet.create({
  header: {
    width: '100%',
    alignItems: 'center',
  },
  content: {
    marginBottom: 60,
    marginTop: 40,
  }
});
