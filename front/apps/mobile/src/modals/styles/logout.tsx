import { StyleSheet } from "react-native";

export default StyleSheet.create({
  text: {
    justifyContent: 'center',
    alignItems: 'center',
    gap: 8,
    marginBottom: 16,
    marginTop: 12,
  },
  header: {
    justifyContent: 'center',
    alignItems: 'center',
  },

});
