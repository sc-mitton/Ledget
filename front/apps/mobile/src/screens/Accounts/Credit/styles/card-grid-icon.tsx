import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  cardIcon: {
    width: 12,
    height: 8,
    margin: 1,
    borderRadius: 2,
    borderWidth: 1.25,
  },
  cardIconRow: {
    flexDirection: 'row',
  }
});

export default styles;
