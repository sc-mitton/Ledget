import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tabBarContainer: {
    position: 'absolute',
    left: 16,
    right: 16,
  },
  scene: {
    position: 'relative',
    zIndex: 10,
  },
});

export default styles;
