import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tabs: {
    flexDirection: 'row',
    marginVertical: 20,
    marginHorizontal: 6,
    gap: 8,
  }
});

export default styles;
