import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  menu: {
    position: 'absolute',
    right: 0,
    top: -16
  },
  reminderIcon: {
    marginRight: -8,
    flexDirection: 'row',
  }
});

export default styles;
