import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: -32
  },
  column: {
    marginRight: 16,
    gap: 8
  },
  infoIcon: {
    marginRight: 5,
  }
});

export default styles;
