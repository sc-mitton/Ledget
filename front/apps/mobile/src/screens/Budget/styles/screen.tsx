import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  scrollView: {
    marginTop: 0,
    zIndex: -1,
  },
  scrollViewContent: {
    paddingTop: 8
  },
  scrollViewSpacer: {
    height: 30,
  }
});

export default styles;
