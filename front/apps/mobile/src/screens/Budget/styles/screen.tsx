import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  scrollView: {
    zIndex: -1,
    marginTop: 64,
  },
  scrollViewSpacer: {
    height: 16,
  }
});

export default styles;
