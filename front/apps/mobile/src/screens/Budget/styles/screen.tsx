import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  scrollView: {
    marginTop: 0,
    zIndex: -1,
  },
  billsContainer: {
    marginTop: -16
  },
  scrollViewSpacer: {
    height: 32
  }
});

export default styles;
