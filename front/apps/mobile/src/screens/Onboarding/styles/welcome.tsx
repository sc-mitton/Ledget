import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  image: {
    marginVertical: 12
  },
  mainContainer: {
    gap: 8
  },
  message: {
    flexDirection: 'row',
  }
});

export default styles;
