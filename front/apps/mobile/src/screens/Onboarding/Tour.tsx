import { View } from 'react-native'
import { OnboardingScreenProps } from '@/types'

const Tour = (props: OnboardingScreenProps<'Tour'>) => {
  return (
    <View>
    </View>
  )
}
export default Tour
