import { View } from "react-native"
import { Widget } from "@/features/widgetsSlice"

const SquareFilled = (props: { account: string, shape: Widget['shape'] }) => {
  return (
    <View>

    </View>
  )
}

export default SquareFilled
