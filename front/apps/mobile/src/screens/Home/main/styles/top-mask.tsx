import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  topMask: {
    position: 'absolute',
    top: 30,
    left: -200,
    right: 0,
    height: 40,
    width: '300%',
    zIndex: 100
  }
});

export default styles;
