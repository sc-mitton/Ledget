import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  main: {
    flex: 1,
    position: 'relative',
    paddingTop: 80,
    paddingLeft: 24,
    paddingRight: 24,
  }
});

export default styles;
