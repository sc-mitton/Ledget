import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  legalFooter: {
    gap: 4,
    opacity: .3,
  },
  legalFooterIOS: {
    marginBottom: 20
  }
});

export default styles;
