import { Box } from '@components'

import { LogoIcon } from '@ledget/media/native'
import styles from './styles/logo'

const Logo = () => (
  <Box style={styles.logo}><LogoIcon /></Box>
)

export default Logo
