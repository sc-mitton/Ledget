import { StyleSheet } from "react-native";

export default StyleSheet.create({
  content: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'flex-start',
    gap: 14,
  },
  cardInfo: {
    flexDirection: 'row',
    gap: 14,
    alignItems: 'center',
  }
})
