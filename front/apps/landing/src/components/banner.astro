---
import '../styles/banner.scss';
import { Image } from 'astro:assets';

import PhoneWebDark from '../../public/phonewebdark3dv12.png';
import PhoneWebLight from '../../public/phoneweblight3dv7.png';
import PlayStoreButton from './buttons/play-store.astro';
import AppStoreButton from './buttons/app-store.astro';
import ArrowLink from './links/arrow-link.astro';
---

<div class="banner">
  <script>
    // Check for the color scheme div element whether it has dark-mode class
    import '../scripts/products.ts';

    const darkMode = document
      .querySelector('.body')
      ?.classList.contains('dark');
    if (darkMode) {
      document.querySelector('.banner--products')?.classList.add('dark');
    } else {
      document.querySelector('.banner--products')?.classList.add('light');
    }
  </script>
  <div>
    <h1>Simplify Your <br /> Finances</h1>
    <span class="sub-header">
      Organize your personal spending and bills, and
      <br />see all of your money in one place.
    </span>
    <div class="buttons">
      <ArrowLink
        hrefTarget={`${import.meta.env.ACCOUNTS_HOST}/register`}
        text="Try for Free"
        className="cta"
      />
      <div>
        <PlayStoreButton />
        <AppStoreButton />
      </div>
      <span>coming soon</span>
    </div>
  </div>
  <div class="banner--products">
    <Image src={PhoneWebDark} alt="Phone with Ledger App" class="dark" />
    <Image src={PhoneWebLight} alt="Phone with Ledger App" class="light" />
  </div>
</div>
