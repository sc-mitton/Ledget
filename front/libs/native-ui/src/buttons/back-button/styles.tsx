import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 0,
    marginLeft: -4,
  },
  chevronContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 4,
  }
});
