import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 10,
    marginLeft: 5,
  },
  chevronContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 4,
  }
});
