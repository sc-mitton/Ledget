import { StyleSheet } from "react-native";

export default StyleSheet.create({
  billCatLabel: {
    borderRadius: 10,
    paddingHorizontal: 8,
    paddingVertical: 1,
    flexDirection: 'row',
    gap: 4
  }
});
