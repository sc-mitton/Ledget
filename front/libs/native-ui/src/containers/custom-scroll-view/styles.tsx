import { StyleSheet } from "react-native";

export default StyleSheet.create({
  scrollIndicator: {
    position: 'absolute',
    borderRadius: 12,
    right: -8,
  },
  container: {
    flex: 1
  }
});
