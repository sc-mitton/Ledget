import { StyleSheet } from "react-native";

export default StyleSheet.create({
  toastStack: {
    position: 'absolute',
    right: 0,
    left: 0,
    zIndex: 1000
  },
  animatedView: {
    position: 'absolute',
  }
});
