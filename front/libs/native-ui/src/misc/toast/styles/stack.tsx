import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  toastStack: {
    position: 'absolute',
    justifyContent: 'center',
    alignItems: 'center',
    right: 0,
    left: 0,
    zIndex: 1000,
  },
});
