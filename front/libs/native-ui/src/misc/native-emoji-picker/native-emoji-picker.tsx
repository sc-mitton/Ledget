import { View } from 'react-native';

import styles from './styles/native-emoji-picker';
import { NativeEmojiPickerProps } from './types';

export function NativeEmojiPicker(props: NativeEmojiPickerProps) {
  return (
    <View style={styles.main}>

    </View>
  );
}


export default NativeEmojiPicker;
