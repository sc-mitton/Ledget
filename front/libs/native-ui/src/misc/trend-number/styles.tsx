import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  trendNumber: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
  },
  icon: {
    alignItems: 'center',
    flexDirection: 'row',
  },
});

export default styles;
