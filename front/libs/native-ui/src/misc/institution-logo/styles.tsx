import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  institutionInitial: {
    position: 'absolute',
  },
  institutionInitialContainer: {
    position: 'absolute',
    top: '50%',
    left: '50%',
    justifyContent: 'center',
    alignItems: 'center',
  },
  defaultLogoBack: {
    opacity: 0.1,
  },
});

export default styles;
