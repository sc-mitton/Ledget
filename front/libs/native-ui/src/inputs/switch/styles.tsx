import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  switch: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    marginVertical: 6,
    width: '100%',
  },
  disabled: {
    opacity: 0.4,
  },
});
