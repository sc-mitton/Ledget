import { StyleSheet } from "react-native";

export default StyleSheet.create({
  switch: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    marginVertical: 4,
  },
  disabled: {
    opacity: .4
  }
});
