app:
    build:
      args:
        - DEV=true
      context: .
    ports:
      - "3000:3000"
    volumes:
      - ./certs:/certs
  portal:
    build:
      args:
        - DEV=true
      context: .
    ports:
      - "3001:3001"
    command: npm start
    volumes:
      - ./certs:/certs
  ory-tunnel:
    build:
      context: ./
    ports:
      - "4000:4000"
  oathkeeper:
    build:
      context: .


